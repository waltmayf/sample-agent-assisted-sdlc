{
  "mcpServers": {
    "gateway": {
      "command": "node",
      "args": [
        "/mnt/plugins/gateway-iam-proxy/index.js",
        "--gateway-url", "{{GATEWAY_URL}}",
        "--region", "{{REGION}}"
      ]
    }
  }
}
